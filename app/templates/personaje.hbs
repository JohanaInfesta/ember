{{outlet}}
{{#link-to "search" class="btn btn-primary"}}
back
{{/link-to}}

{{#each model.data.results as |search|}}
{{character-item name=search.name image=(concat search.thumbnail.path "." search.thumbnail.extension) description=search.description id=search.id}}
{{/each}}
